// TaskName is now a plain string read from the workflow schema's "activityName" property.
// The enum and TaskNameExtensions.FromActivityId() mapping have been removed
// to make task names fully data-driven.
